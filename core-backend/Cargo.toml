[package]
name = "core-backend"
version = "1.3.3"
edition = "2021"

[dependencies]
tokio = { version = "1.28", features = ["full", "io-std", "io-util"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# Note: tree-sitter and lancedb are specified in the tech stack.
# We will scaffold the integrations cleanly in src/.
